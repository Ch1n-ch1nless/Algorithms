#include <cassert>
#include <iostream>
#include <vector>



int main()
{
    int n = 0;
    int m = 0;

    std::cin >> n >> m;

    std::vector<int> coords(n);

    for (int i = 0; i < n; ++i)
    {
        std::cin >> coords[i];
    }

    int 

    return 0;
}